import { NavBar, Footer } from '../containers';

import '../css/Reads.css';

const Reads = () => {
    return(
        <div>
            <NavBar/>
            <Footer/>
        </div>
    )
}

export default Reads;